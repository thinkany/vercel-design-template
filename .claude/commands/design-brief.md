---
description: Design from a brief, one pass from a natural-language design request with references (a site to model, a color source, fonts) to an on-brand v01 design. The "Get Designing" path.
argument-hint: "the brief, e.g. 'a site like stripe.com, colors from stripe, Playfair + Inter'"
---

This is a **licensed thinkany design skill**. Its playbook is delivered by the
thinkany design app when the project is open there with an active Design license;
this file is only the placeholder that lets `/design-brief` autocomplete.

**Do this now:** call the `load` tool of the `skills` MCP server with
`{ "name": "design-brief" }` and follow the playbook it returns, exactly, for the
designer's request. Do not improvise the procedure from memory: it exists so
that the result is consistent.

If that tool is not available, or it says the playbook is not available, tell the
designer in one plain sentence that `/design-brief` runs from the thinkany design app
with a Design license (Keys & Licenses in the app), and stop.
