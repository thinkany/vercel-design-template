---
description: Build or edit a design page (the post-setup design phase), the condensed authoring contract + progress protocol, so you go straight to designing without re-deriving the rules
---

This is a **licensed thinkany design skill**. Its playbook is delivered to the
session by the thinkany design app when the project is open there with an active
Design license; this file is only the placeholder that lets `/design` autocomplete.

If you are reading this text as a command's content, the playbook was NOT loaded.
Tell the designer, in one plain sentence, that `/design` runs from the thinkany
design app with a Design license (Keys & Licenses in the app), and stop. Do not
improvise the procedure from memory: it exists so that the result is consistent.
