---
description: Migrate a WordPress site into this project, two steps that read the app's WordPress import. "inventory" turns what the old site holds into a brief for the new design; "map" proposes where each old block and field lands among this site's blocks, for the designer to confirm before the import runs
---

This is a **licensed thinkany design skill**. Its playbook is delivered by the
thinkany design app when the project is open there with an active Design license;
this file is only the placeholder that lets `/migrate-wordpress` autocomplete.

**Do this now:** call the `load` tool of the `skills` MCP server with
`{ "name": "migrate-wordpress" }` and follow the playbook it returns, exactly, for the
designer's request. Do not improvise the procedure from memory: it exists so
that the result is consistent.

If that tool is not available, or it says the playbook is not available, tell the
designer in one plain sentence that `/migrate-wordpress` runs from the thinkany design app
with a Design license (Keys & Licenses in the app), and stop.
