---
description: Promote an approved design into site blocks + content (the start of a Site Build), each section becomes a block with a props schema, its copy and images move into content/, the header/footer become the site chrome, then the site builds
---

This is a **licensed thinkany design skill**. Its playbook is delivered to the
session by the thinkany design app when the project is open there with an active
Design license; this file is only the placeholder that lets `/promote-blocks` autocomplete.

If you are reading this text as a command's content, the playbook was NOT loaded.
Tell the designer, in one plain sentence, that `/promote-blocks` runs from the thinkany
design app with a Design license (Keys & Licenses in the app), and stop. Do not
improvise the procedure from memory: it exists so that the result is consistent.
