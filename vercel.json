{
  "buildCommand": "pnpm run build",
  "outputDirectory": "dist",
  "installCommand": "PUPPETEER_SKIP_DOWNLOAD=true pnpm install --no-frozen-lockfile",
  "framework": "vite",
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "X-Robots-Tag",
          "value": "noindex, nofollow"
        }
      ]
    }
  ]
}
