node_modules/
dist/
# Local npm lockfile — throwaway. Vercel builds with pnpm (see vercel.json /
# pnpm-lock.yaml); npm install locally is only for the dev server.
package-lock.json
.DS_Store
*.local
.vercel
# .env is committed (public brand config, no secrets).
# Secrets/local overrides go in .env.local — already ignored by *.local above.
.claude/settings.local.json
pnpm-workspace 2.yaml

# Figma export output (local, throwaway)
figma-export/

# Pre-update snapshots for one-click revert (local, throwaway)
.upgrade-backup/

# Packaging (electron-builder): the generated pristine template snapshot shipped
# inside the .app, and the built .app/.dmg output — both build artifacts.
desktop/template/
dist-app/

# Exported company (agency) profile — a personal, portable artifact the designer
# moves elsewhere to reuse; not meant to be committed into a client project.
company-profile.json

# site build target (Astro)
dist-site/
site/.astro/
